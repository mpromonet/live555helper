name: C/C++ CI windows

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v5
    - name: cmake
      run: cmake -G Ninja .
    - name: make
      shell: cmd
      run: ninja
    - name: cpack
      run: cpack
