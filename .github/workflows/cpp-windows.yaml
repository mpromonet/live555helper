name: C/C++ CI windwos

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v5
    - name: cmake
      run: cmake .
    - name: make
      shell: cmd
      run: | 
          type Makefile
          make
    - name: cpack
      run: cpack
